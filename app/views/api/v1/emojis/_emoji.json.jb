{
  id: emoji.id,
  name: emoji.name,
  description: emoji.description,
  number_of_downloaded: emoji.number_of_downloaded,
  images: {
    large_url: emoji.image.large.url,
    thumb_url: emoji.image.thumb.url,
    slack_url: emoji.image.slack.url,
  },
  tags: emoji.tags.map do |tag|
    { id: tag.id, name: tag.name }
  end,
  user: { id: emoji.user.id, name: emoji.user.name },
}
